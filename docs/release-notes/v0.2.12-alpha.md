# v0.2.12-alpha

Study Anything v0.2.12-alpha packages the platform-agent integration work from `main` into a
published alpha release. It turns the API/Skill launch path into a more importable platform tool
surface for Codex, Kimi-compatible function-calling agents, WorkBuddy-style workspaces, and private
Agent platforms.

## Highlights

- Adds `platform/study-anything-platform-tools.json`, a constrained machine-readable tool manifest for
  the minimum learning loop.
- Adds generated platform import assets:
  - `platform/generated/study-anything-platform-openapi.json`
  - `platform/generated/study-anything-openai-tools.json`
  - `platform/generated/study-anything-tool-catalog.md`
- Adds `python3 scripts/generate_platform_agent_assets.py` with `--check` drift detection.
- Adds `python3 scripts/verify_platform_agent_tools.py`, which drives a real API learning loop from
  the manifest and verifies redacted Agent audit/eval evidence.
- Extends CI and release checks so generated assets and platform-tool verification stay synchronized.
- Updates default published-image tags to `v0.2.12-alpha`.

## First Launch

Install Docker Desktop or Docker Engine with the Compose plugin, start the daemon, then run:

```bash
python3 scripts/setup_env.py
USE_PUBLISHED_IMAGES=true ./scripts/launch_self_host.sh
```

Open:

- API docs: http://localhost:8000/docs
- API health: http://localhost:8000/v1/health
- System status: http://localhost:8000/v1/system/status

Maintainers can verify the public API image end to end with:

```bash
python3 scripts/verify_published_image_launch.py --tag v0.2.12-alpha
```

## Platform Agent Assets

Use the constrained manifest when building an integration:

```text
platform/study-anything-platform-tools.json
```

Use the generated OpenAPI file for HTTP tool importers and the generated OpenAI-compatible tools JSON
for function-calling Agent platforms:

```text
platform/generated/study-anything-platform-openapi.json
platform/generated/study-anything-openai-tools.json
```

Validate a running integration with:

```bash
API_BASE=http://127.0.0.1:8000 python3 scripts/verify_platform_agent_tools.py
```

The platform tool surface intentionally excludes Agent provider setup, plugin installation, encrypted
sync export, PMF export, and other management APIs.

## Upgrade

Create a local backup before upgrading:

```bash
python3 scripts/self_host_data.py backup
```

Then pull the release image:

```bash
STUDY_ANYTHING_IMAGE_TAG=v0.2.12-alpha \
USE_PUBLISHED_IMAGES=true \
./scripts/launch_self_host.sh
```

## Known Limitations

- The alpha remains self-host-first. Managed hosting, accounts, billing, Publish, and Teams are not
  included yet.
- There is no shippable standalone Web UI in this release. The supported product path is API, Skill,
  CLI, and platform-agent integration.
- Generated platform tools are integration assets, not hosted marketplace listings or submitted
  third-party plugin packages.
- `agent-eval` exports a redacted eval artifact; it does not run judge models by itself.
- Kimi browser-only chat still cannot operate local scripts or reach `127.0.0.1`; use the documented
  local gateway or a terminal-capable Agent.
- Study Sync is still a local encrypted package boundary, not hosted remote sync.
- Recovery remains manual. API status surfaces do not execute destructive restore operations.
- Plugin installation remains local and explicit; remote marketplace distribution is not implemented.
- First-run GHCR downloads can take several minutes on slower connections.
